/** Dev-only overrides for simulating native behavior from a plain browser. */
export const ENV_FORCE_NATIVE = "NEXT_PUBLIC_FORCE_NATIVE";
export const ENV_PLATFORM_OVERRIDE = "NEXT_PUBLIC_PLATFORM";

/** Key zustand's auth persistence writes under, shared across storage engines. */
export const AUTH_STORAGE_KEY = "nawa-napam-auth";
